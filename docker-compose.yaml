version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./apps:/app/apps
      - ./:/app
    tty: true
    links:
      - voicevox
      - admin

  voicevox:
    image: voicevox/voicevox_engine
    # ports:
    #   - 50021:50021
    tty: true

  admin:
    build:
      context: ./apps/admin
      dockerfile: Dockerfile
    volumes:
      - ./apps/admin:/app
      - node_modules:/app/node_modules
    command: yarn dev
    tty: true

volumes:
  # to ignore dir.
  apps:
  node_modules:
